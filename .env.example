# ============================================
# 🔐 環境變數範例文件
# ============================================
# 
# ⚠️ 重要提醒：
# 1. 這個文件只是範例，不包含真實數據
# 2. 真實的環境變數應該存放在：
#    - Replit Secrets (開發環境)
#    - Vercel Environment Variables (正式環境)
# 3. 絕對不要把真實的 API Key 或 Token 寫在這裡
#
# ============================================

# Firebase 客戶端配置（前端使用）
# 從 Firebase Console → Project Settings → General 取得
NEXT_PUBLIC_FIREBASE_API_KEY=your-firebase-api-key
NEXT_PUBLIC_FIREBASE_AUTH_DOMAIN=your-project.firebaseapp.com
NEXT_PUBLIC_FIREBASE_PROJECT_ID=your-project-id
NEXT_PUBLIC_FIREBASE_STORAGE_BUCKET=your-project.appspot.com
NEXT_PUBLIC_FIREBASE_APP_ID=your-app-id

# Firebase Admin SDK（伺服器端使用）
# 從 Firebase Console → Project Settings → Service Accounts 下載
# 可以是完整 JSON 或 Base64 編碼
FIREBASE_SERVICE_ACCOUNT_JSON={"type":"service_account","project_id":"..."}

# LINE 官方帳號配置
# 從 LINE Developers Console 取得
LINE_CHANNEL_ID=your-line-channel-id
LINE_CHANNEL_SECRET=your-line-channel-secret

# LINE LIFF 配置（前端使用）
# 從 LINE Developers Console → LIFF 取得
NEXT_PUBLIC_LINE_LIFF_ID=your-liff-id

# LINE Bot 配置（Webhook 使用）
LINE_BOT_CHANNEL_ID=your-bot-channel-id
LINE_BOT_CHANNEL_SECRET=your-bot-channel-secret
LINE_BOT_ACCESS_TOKEN=your-bot-access-token

# Vercel 部署配置（選用）
VERCEL_ADMIN_API_KEY=your-vercel-api-token
VERCEL_PROJECT_ID=your-vercel-project-id
VERCEL_ORG_ID=your-vercel-org-id

# Session 配置（可選）
SESSION_SECRET=your-random-secret-string
